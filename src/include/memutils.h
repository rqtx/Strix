/*
 *  memutils.h
 */
#ifndef MEMUTILS_H
  #define MEMUTILS_H

#include <stdio.h>
#include <stdlib.h> 
#include <string.h>

 void memalloc( void ** p_ptr, size_t p_size, const char * const p_func );

#endif
